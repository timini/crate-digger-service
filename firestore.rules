rules_version = '2';
// Only the service, using its own credentials, reads or writes data.
// No client may use Firestore directly.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
